const db = require('../../../config/db');

class UsuariosDB {
  static async getUsuarios() {
    // Aquí es donde ejecutarías las consultas a la base de datos
    // return await db.execute('SELECT * FROM usuarios');
  }
  // Implementa el resto de los métodos aquí...
}

module.exports = UsuariosDB;
